@model AdoptameLiberia.Models.LoginViewModel

@{
    ViewBag.Title = "Iniciar sesión";
}

@section Styles {
    <link href="~/Content/estilos/Login.css" rel="stylesheet" />
}

<div class="auth-shell">
    <div class="auth-card">
        <div class="auth-visual">
            <div class="auth-visual-text">
                <span class="auth-kicker">ARAC Refugio</span>
                <h2>Bienvenido de nuevo</h2>
                <p>Ingresa para gestionar tus adopciones, favoritos, donaciones y acceso a tu cuenta.</p>
            </div>
            <div class="auth-visual-img-wrap">
                <img src="~/Content/img/Cachorro.png" alt="Cachorro ARAC" />
            </div>
        </div>

        <div class="auth-form-side">
            <h1>Iniciar sesión</h1>
            <p class="auth-subtext">¿No tienes cuenta? <a href="@Url.Action("Register", "Account")">Regístrate aquí</a></p>

            @using (Html.BeginForm("Login", "Account", FormMethod.Post, new { role = "form", @class = "auth-form" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger auth-summary" })

                <div class="auth-group">
                    @Html.LabelFor(m => m.Email, "Correo electrónico")
                    @Html.TextBoxFor(m => m.Email, new { @type = "email", @placeholder = "correo@ejemplo.com", @class = "form-control", autocomplete = "username" })
                    @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                </div>

                <div class="auth-group">
                    @Html.LabelFor(m => m.Password, "Contraseña")
                    @Html.PasswordFor(m => m.Password, new { @placeholder = "Ingresa tu contraseña", @class = "form-control", autocomplete = "current-password" })
                    @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                </div>

                <label class="auth-check">
                    @Html.CheckBoxFor(m => m.RememberMe)
                    <span>Recordarme</span>
                </label>

                <button type="submit" class="auth-submit">Entrar</button>

                <div class="auth-links">
                    <a href="@Url.Action("ForgotPassword", "Account")">¿Olvidaste tu contraseña?</a>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}